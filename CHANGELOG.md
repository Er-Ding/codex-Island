# Windows 更新日志

当前使用方法见 [README](README.md)。本地构建不等于已发布 GitHub Release。

## 未发布

### 2026-09-12 · Windows 0.4.1 修复启动前任务漏计

- 问题：先在 Desktop 开始任务、后启动 Island 时，部分后台任务没有重新广播初始通知，而 Island 只扫描部分本地索引，导致遗漏。
- 修复：补充 Desktop 会话与页签缓存、项目归属及后台子任务发现，区分 UI scope 和实际设备；启动时主动请求已有任务的当前快照。
- 恢复：首次订阅无回应时自动退避重试；缓存重扫保留实时发现的任务，断线重连后补订阅；直接收到有效任务快照也能补齐遗漏通知。
- 覆盖：大量历史候选分批轮换，已确认活动任务保留；真实任务事件不受历史探测冷却限制。缓存中的标题和会话记录不会直接计入运行任务。
- 验证：99 项核心／任务管道检查通过，包含 14 项启动补齐回归检查；覆盖 1300 个历史候选、延迟快照、安静任务重连和冷却期间的实时通知。Windows 编译 0 警告、0 错误，75 项界面／导航保护检查通过。
- 实机：保持已有 Codex 任务运行，退出 Island 后依次启动旧／新版诊断；旧版发现 2 个运行任务，新版发现 3 个，确认补回遗漏任务。

### 2026-09-12 · 同步 macOS 任务功能到 Windows

- 原因：Windows 0.3.3 仅显示额度，尚未包含远端 macOS `main`（`9c1b658`）的任务概览与会话导航。
- 功能：增加任务状态、来源设备、计划进度、缩略徽标、独立同步及断线保留；点击打开 Desktop 会话，并按 Windows 环境适配 VS Code、终端与侧边聊天定位。
- 交互：缩略及展开动画期间可直接接住顶部拖动；拖动中 Esc／右键取消；任务徽标按下时固定目标，拖动不触发导航，双击额度区域复位。
- 额度：周额度改为显示北京时间的具体刷新日期，其他周期保留倒计时。
- 文档：更新 Windows 使用说明及平台分支现状，不将旧本地 macOS 分支视为最新功能来源。
- 接口依据：本机 Windows Codex Desktop `26.908.4834.0` 的已有任务管道、会话链接与侧边页签结构；只读取所需任务摘要，不缓存完整任务输入、推理、工具参数或输出。
- 验证：Windows x64 编译通过（0 警告、0 错误）；85 项核心／任务管道检查、75 项界面／导航保护检查、34 项真实鼠标输入检查通过。已连接本机 Desktop 任务流并读取运行状态，真实额度读取通过。演示界面已检查排版。
- 交付检查：0.4.0 自包含单文件便携包构建通过，最终可执行文件再次通过 75 项界面／导航保护检查及真实任务连接诊断；尚未推送分支或创建 GitHub Release。
- 导航验证范围：本轮验证接口源码与隔离拒绝路径，没有点击用户真实会话逐一验收 Desktop、VS Code、终端与侧边聊天跳转；这些入口的实际成功率仍受目标应用版本和窗口状态影响。
- 已知限制：任务仅覆盖 Desktop 已加载且可同步的会话。原窗口、终端页签或侧边聊天不能唯一定位时提示原因或回退 Desktop；Windows 与 macOS 的应用自动化接口不同。多屏混合 DPI、不同 Desktop／VS Code／Terminal 版本仍需对应环境验收。
